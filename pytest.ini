[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

markers =
    unit: unit tests, no external dependencies
    e2e: end-to-end tests, exercise the full stack
    integration: integration tests, cross real dependencies (db, apis, etc.)
